# hono-sse

Minimal full-stack chat using Hono, Bun, and Gemini AI.

## Setup
1. `bun install`
2. Create `.env` with `GEMINI_API_KEY=your_key` (PORT is optional, defaults to 3000)
3. `bun run dev`

Open [http://localhost:3000](http://localhost:3000) (or your custom port).
